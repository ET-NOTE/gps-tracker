# shell 스크립트는 항상 LF — Windows 에디터/git autocrlf 로 CRLF 섞이면 wsl bash 가 깨짐
*.sh text eol=lf

# bash heredoc 안에 임베드되는 SQL 도 LF
*.sql text eol=lf

# Windows 전용은 CRLF
*.bat text eol=crlf
*.ps1 text eol=crlf
