# GPS 외부 안테나 진단 — L86 `ANTSTATUS=OPEN`

_2026-07-02~03. sss/aa(L86) 실측 기반._

## ✅ 해결됨 (2026-07-03) — 원인 = 안테나 검출 LED 가 bias 전류를 소모
**aa 수리 후 `$GPTXT,01,01,02,ANTSTATUS=OK*3B` 확인 (OPEN→OK).**
- **근본원인**: "안테나 연결 유무 표시 LED" 를 안테나 급전 라인에 엮어서, **LED 가 bias 전류를 먹어버림** → 모듈이 볼 안테나(LNA) 전류가 부족/왜곡 → OPEN.
- **수정**: 그 **LED 를 급전 라인에서 분리** → 전류가 안테나로 정상 유입 → 모듈 검출 → OK.
- **진단 체인 검증**: (1) ANTSTATUS=전압 아닌 **전류** 판정(§4-1) (2) 보드 비교 `LC86G=OK / L86=OPEN`(§4-3) — L86 보드에만 있던 LED 로드가 범인. bias **전압**은 있었으나 LED 가 **전류**를 훔쳐 OPEN 이었음.
- 아래 §1~4 는 조사 과정 기록(참고용).

### ★★ 최종 확정 (2026-07-03) — 결정 요인 = 안테나 '종류'(전류 소모)
- **같은 sss 보드에 안테나만 교체 실험**: sss 원래 안테나 → `OPEN`. aa 의 안테나로 교체 → **즉시 `OK`**.
- **결론**: OK/OPEN 을 가르는 건 보드가 아니라 **안테나가 bias 전류를 끄느냐**. 
  - **액티브 안테나(LNA·전류 소모)** → 모듈이 외부로 검출 → `OK`.
  - **패시브/무전류 안테나** → 전류 미검출 → `OPEN`(내장 패치로 폴백).
- "전압≠전류" 진단의 최종 증명(§4-1). LED 전류 도둑은 그 전류를 더 깎던 별개 요인.
- **운영 결론**: 외부 안테나 OK 를 원하면 **전 단말 액티브 안테나로 통일** + (LED 를 급전 전류 경로에서 분리). 단말 대수만큼 액티브 안테나 필요.

---

## 1. 증상
- 모듈 각인: **Quectel L86-M33** (Q1-A0437). MT3339 기반, **패치 안테나 내장** + 외부 액티브 안테나 지원 + 안테나 detection.
- 외부 안테나 연결 + 보드 LED 점등. **그런데** 모듈 안테나 상태 = **OPEN**.
- GPS 자체는 정상 (운행 중 sat=12 fix).

## 2. 원인 (확정)
모듈 원시 NMEA (펌웨어 `[GPS raw-ant]` 로깅으로 캡처):
```
$GPTXT,01,01,02,ANTSTATUS=OPEN*2B
```
MTK에서 **`OPEN` = 안테나 급전선 개방 = DC 전류 미검출**.

**LED 점등과 OPEN이 모순이 아닌 이유 — 서로 다른 걸 측정:**
| | 측정 대상 | 값 |
|---|---|---|
| LED | 안테나 물리 연결 / 전원경로 | 점등 |
| ANTSTATUS(NMEA) | 안테나 **DC 전류** | OPEN(0) |

원인은 둘 중 하나:
1. **외부 안테나가 패시브(LNA 없음)** → DC 전류 안 씀 → 정상적으로 OPEN. (GPS는 작동.)
2. **액티브 안테나인데 보드가 바이어스(3.3V)를 안테나 포트에 급전 안 함** → LNA 무전원 → 전류 0 → OPEN.

→ L86은 **내장 패치 안테나**가 있어, 현재 fix는 **내장으로 잡히는 중**이고 외부(OPEN)는 실사용 안 되는 상태일 가능성이 큼.

**소프트웨어로는 이 급전을 만들 수 없음** (바이어스는 순수 하드웨어). ANTSTATUS는 모듈이 전류를 재는 것이라 전류가 없으면 무조건 OPEN.

## 3. 하드웨어 개발자에게 증명하는 법

### (A) 펌웨어 증거 — 모듈 자체 판정
로그의 `$GPTXT,01,01,02,ANTSTATUS=OPEN` 제시. GNSS 칩이 급전선 전류를 못 본다는 모듈 원문(파싱 오류 아님).

### (B) 멀티미터 측정 — 물리적 확증 ★
**측정: 외부 안테나 커넥터(u.FL/IPEX 또는 SMA) 중심핀(RF) ↔ GND, DC 전압.**
- 멀티미터 **DC V** 모드
- (+) = 커넥터 **중심핀**, (−) = 커넥터 **금속 쉘/GND**

| 읽힘 | 의미 |
|---|---|
| **~2.8–3.3V** | 보드가 안테나 **바이어스 급전 중** → OPEN이면 안테나/센스회로 쪽 문제 |
| **~0V** | **바이어스 없음** → 액티브 안테나 무전원 → **OPEN 확정 = 보드가 안테나 급전 안 함** (결정적 증거) |

### (C) 배선/센스 회로 확인
- **전원 OFF**에서 커넥터 중심핀 ↔ 모듈 **RF_IN** 도통 확인 (외부가 모듈 RF 입력에 실제 연결됐는지).
- 커넥터 근처 **피드 인덕터 + 센스 저항**(액티브 안테나 detection 기준회로) 존재 여부.

### (D) 설계 근거
**"L86 Hardware Design" 문서의 액티브 안테나 detection 기준회로**(피드 인덕터 + 센스 저항 + V_ANT) 대조. 이 회로가 없으면 ANTSTATUS는 항상 OPEN.

## 4. 개발자 전달 멘트 (요약)
> L86-M33가 `ANTSTATUS=OPEN`을 계속 방출(로그 有). 외부 안테나 커넥터 중심핀 DC 전압이 **0V** → 액티브 안테나 바이어스 미급전. L86 HW Design의 안테나 급전/센스 회로가 빠졌거나 미동작. 현재 fix는 내장 패치로 추정. 외부 액티브 안테나를 쓰려면 커넥터 RF 라인에 **3.3V bias-tee(피드 인덕터)+센스 저항** 추가 요망.

## 4-1. 업데이트 (2026-07-03) — 개발자 "DC 흐른다" 회신 후 aa 재확인
- **개발자 회신**: sss·aa **양쪽 다 안테나쪽으로 DC 흐른다** (= bias 전압 급전 확인).
- **그런데 aa 재부팅 실측**(RTS 리셋 후 부팅 캡처): `[GPS] antenna=OPEN (GPTXT, boot+13.2s)` — **여전히 OPEN**.
- **해석 정정 (핵심)**: ANTSTATUS 는 **전압이 아니라 전류**를 판정. bias **전압**이 커넥터에 있어도 안테나가 그 전류를 실제로 끌어가지 않으면(전류 ~0) OPEN. (개방 회로 = 전압 有, 전류 無.) 따라서 위 §3(B) 표의 "~3.3V=급전중이면 OK 여야" 전제는 **부정확** — 전압만으론 OK 안 됨.
- **개발자 확인 요청**:
  1. 외부 안테나가 **액티브(LNA·전류 소모)인가 패시브(전류 안 씀)인가?** 패시브면 OPEN 정상(RF 로 동작, 무시 가능).
  2. 액티브인데 OPEN → bias 전압은 있는데 **LNA 로 전류 안 흐름**. 급전 경로에 **DC 블록 캡이 전류 센스 뒤**에 있거나 **피드 인덕터/센스 저항 값**이 검출 임계와 불일치 의심.
  3. **결정적 측정**: 커넥터 DC "전압" 말고, **안테나 연결 상태에서 급전 라인 전류(또는 센스 저항 양단 전압)** 측정 → 실제 전류 유무 확정. 전류≈0 이면 액티브 아님/경로 끊김.
- **요지**: "DC 가 커넥터까지 온다" ≠ "안테나 LNA 로 전류가 흘러 모듈이 센싱한다".

## 4-2. 개발자용 안테나 진단 펌웨어 (2026-07-03) — aa 에 업로드됨
HW 개발자가 시리얼 모니터로 ANTSTATUS 를 **실시간** 보며 안테나/급전을 고칠 수 있는 전용 펌웨어.
- **위치**: `arduino/zz_antenna_diag/zz_antenna_diag.ino` (자립형, LTE/모션/슬립 미동작 → 안테나만 깔끔).
- **빌드/플래시**: `arduino-cli compile/upload -p COMxx --fqbn esp32:esp32:esp32c3:CDCOnBoot=cdc`
- **모니터**: `arduino-cli monitor -p COMxx --config baudrate=115200` (또는 Arduino IDE 115200, **인코딩 UTF-8** 권장 — 한글 정상 표시. ASCII 핵심값은 어디서든 읽힘).
- **동작**: 2초마다 `[ANT] 현재=OPEN | emissions=N | raw: $GPTXT,...ANTSTATUS=OPEN*2B` 하트비트. 안테나/급전 바꿔 상태 변하면 `>>> ANTSTATUS 변화! status=OK` 블록이 크게 뜸. (모듈이 ~1s 마다 재방출 → 변화 1~2초 내 포착.) 강제 재확인=보드 리셋.
- **판독**: OPEN=전류 미검출 / OK·USING=정상 전류 / SHORT=단락. **전압≠전류** 주의(§4-1).
- **슬립 안 함** → 모니터 연결 유지. 안테나 확정 후엔 aa 를 정식 필드본으로 재플래시 필요.

## 4-3. ★ 결정적 단서 — 보드별 비교 (2026-07-03)
동일 외부 안테나로 **모듈/보드별 ANTSTATUS 실측**:
| 디바이스 | 모듈 | ANTSTATUS | 원시 |
|---|---|---|---|
| sss | L86 (9600) | **OPEN** | `$GPTXT,01,01,02,ANTSTATUS=OPEN*2B` |
| aa (동료 전달용) | L86 (9600) | **OPEN** | `$GPTXT,...ANTSTATUS=OPEN` |
| 구형(이름 폐기) | **LC86G (115200)** | **OK_EXT** ✅ | `$PQTMANTENNASTATUS,3,1,2,2*51` (status=2 NORMAL, source=2 external) |

**결론**:
- ✅ **외부 액티브 안테나 + 급전 회로 자체는 정상** — LC86G 보드가 실제로 `OK_EXT`(외부 전류 검출)를 내므로 안테나·bias 는 살아있음.
- ❌ **문제는 안테나가 아니라 L86 보드(aa·sss)의 안테나 급전/센스 회로** (또는 L86 의 전류 검출 임계). 개발자가 준 "DC 전압"이 L86 보드에선 모듈이 감지할 **전류**로 이어지지 않음.
- → **LC86G 보드를 known-good 기준**으로 aa(L86) 의 bias-tee(피드 인덕터)·센스 저항 값/배치/DC블록 캡 위치를 대조하면 원인 특정 빠름.

## 5. 소프트웨어 측 가능/불가
- ❌ 바이어스 급전 생성 (하드웨어 전용).
- ✅ 진단 펌웨어: `zz_antenna_diag` (위 §4-2) — 개발자 실시간 관찰용. + 본 펌웨어 `gps.cpp` 의 `[GPS raw-ant]`(변화 시 1회).
- ✅ (선택) telemetry `antenna` 필드에서 "OPEN"을 "no-current(passive/no-bias)" 로 라벨 정리해 혼동 감소.
